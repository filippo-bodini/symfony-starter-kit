# Sparkfabrik - Symfony challenge starter kit

This a starter-kit provided from Sparkfabrik and is intended to be used just for internal usage
and should not re-used, open-sourced or shared with anyone else if not clearly expressed.

## Usage

You can start the project by running `make`, it will build and run a bare Symfony application.
Then you can access it from `http://localhost:8000` from your local web browser, usually it takes 1 minute
for the webserver to start, you can anyway follow the log with `docker-compose logs -f app`.

